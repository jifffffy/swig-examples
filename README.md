# CMake-SWIG-java-gradle example

## Introduction

A gradle project with the CMake build system using the CLion IDE.

## Build instructions

To build the project, run:
```
cmake
./gradlew clean build
```